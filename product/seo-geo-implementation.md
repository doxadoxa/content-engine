# SEO/GEO implementation and verification

> Private pilot evidence is retained locally and is not included in this public repository. References below describe the generic capability and its open production boundary.

> The owner rejected the existing-page-first experience after using it. The earlier technical completion below does not constitute product acceptance. The current corrective scope is [content autopilot](content-autopilot-correction.md), with automatic scheduled article publication and optional review first.

Started: 15 September 2026. This document records the earlier implementation and its evidence. The current product behavior, corrective review and acceptance status are maintained in [content autopilot correction](content-autopilot-correction.md); historical entries below that retire articles/planning or make page audits primary are superseded. Owner + Codex.

The user's implementation instruction covers the complete SEO/GEO focus plan. The earlier conditional AI-answer accuracy and fact-maintenance extensions are both included in this implementation; neither will be left as a placeholder or merely scheduled for later. Explicitly excluded products (social, backlink networks, CRM suite, Maps/reviews, arbitrary page builders, and agency tooling) remain outside this focused product.

## Delivery phases

| Phase | Work | Status | Required review |
|---|---|---|---|
| 0 | Verify pilot purchase flow, existing receiver, page set, local runtime, and integration access | In progress | Independent discovery critique |
| 1 | Remove social execution and UI, focus navigation/marketing, align onboarding and review defaults | Complete | Independent critique approved after fixes; final combined gate passed |
| 2 | Tracked pages, snapshots, confirmed facts, query/page readings, purchase collection/reconciliation | Complete (software and local verification) | Independent critique approved; final combined gate and visible local checks passed; production collection remains an external pilot gate |
| 3 | Ranked opportunities, bounded changes, approval/revision lifecycle, assisted publishing and verification | Complete (software and local verification) | Independent critique approved; 2,006 tests and browser workflow passed; actual pilot publication still A17 |
| 4 | WordPress and custom existing-page publication, conflicts, idempotency, verification and recovery | Complete (software and local verification) | Independent root/subagent reviews, 54 tests / 405 assertions and real HTTP service/article journeys passed; production rollout remains A17 |
| 5 | Purchase performance, baseline/follow-up, stable AI samples, billing/allowances and focused launch offer | Complete (software and local verification) | Independent measurement/economics/entitlement critiques approved; later authorized browser/provider checks completed; current article offer and gates are in the correction document |
| 6 | Full-answer accuracy checks and propagation of changed confirmed facts | Complete (software and local verification) | Independent critiques approved; populated synthetic browser walkthrough and one real metered synthetic checker call completed |
| 7 | End-to-end user flows, complete test/lint/type/build gates, requirement audit, open decisions | Software gates complete; production pilot/outcome dependencies remain | Corrective 2,230-test gate, subsequent scoped billing regression gate, all lint/type/build checks and authorized browser/provider validation recorded in the correction document |

Do not label a phase complete until its implementation, relevant checks, live local flow, and subagent critique are recorded. Tests run serially against the isolated Docker test database. Frontend/static checks may run in parallel.

## Requirement ledger

Each original implementation checkbox is retained here. Evidence links/test names and concrete results replace Pending as work is verified. Narrative acceptance gates and cross-cutting requirements appear below so checking boxes alone cannot establish completion.

| Requirement | Required behaviour | Evidence / status |
|---|---|---|
| F0.01 | Inspect a pilot site and its relevant project configuration. Select roughly 5–20 initial service/pricing/supporting article pages and one priority locale; record the actual market and language. | Verified privately: a bounded pilot page set has preserved canonical identities. The pilot details are retained locally. |
| F0.02 | Establish where a purchase becomes authoritative in the booking/payment process. Identify the source of sale status, transaction identity, actual value/currency, and, if available, first-time-customer status. | Bridge tests verify that authoritative paid receipts are distinct from bookings and completion. Stable IDs, deposits, refunds and customer chronology are covered. |
| F0.03 | Inspect available Search Console/GA4 access and existing tracking. The owner's report that purchases are not tracked is the starting assumption; do not invent historical purchase attribution. | A private, owner-authorized read-only inspection was completed. Avyo Google OAuth and GA4 access remain unverified; historical purchases remain unavailable. |
| F0.04 | Verify the custom site's existing receiver and what it supports: new articles, article updates, and/or edits to existing service pages. Specify an assisted handoff for unsupported updates. Existing webhooks do not automatically provide an editable page snapshot or conflict checking. | Custom article receiver inspected; a new existing-service/article receiver with exact source/revision, conflict checking and guarded recovery is implemented and verified locally. Production setup remains explicit; assisted handoff is available. |
| F0.05 | Prepare one concrete recommendation manually using the existing engine; record why it is worth doing and which actual business facts support it. | A private sourced owner-review question was saved in Plan. Its supporting fact still needs owner confirmation before wording or live publication. |
| F0.06 | Identify a representative WordPress test site/page set for F3. WordPress is the first native integration, regardless of the pilot implementation. | Representative WordPress 7.1/PHP 8.3 fixture, core page/post, Gutenberg and Yoast ownership cases. Receiver integration tests and main Avyo service/article publication, verification, duplicate reconciliation and recovery journeys passed in phase 4. |
| F0.07 | Record the baseline plan, tracking start date, candidate unchanged comparison pages, and known commercial changes. | Private comparison windows and candidate pages were recorded. Purchase tracking start and known commercial changes remain unknown; candidate pages are not certified unchanged. |
| F1.01 | Make Overview, Plan, Content, and Performance the main navigation for the focused offer. Put audits, detailed AI visibility, delivery logs, and setup behind relevant pages/settings. | Implemented and visibly verified: four main navigation entries; Plan now advances to the phase 3 opportunity view. Detailed tools remain contextual. |
| F1.02 | Remove social promises and broad “organic growth team” language from the new landing page, onboarding, demonstration content, and future plan comparison. | Implemented and verified in phase 1 marketing/onboarding/assistant source review and browser checks; social promises removed. |
| F1.03 | Align the trial copy with the actual card/checkout sequence. Explain that the trial demonstrates proposed work and usability, not organic-growth results. | Trial copy aligned with current checkout sequence in phase 1; versioned focused package and final allowance wording in phase 5. |
| F1.04 | Remove social from the active experience, including navigation, onboarding, assistant tools, direct routes, manual generation, and plan inclusions. Nobody currently depends on it; an ongoing internal social product is not required. | Implemented: retired social routes/actions/assistant capabilities return no active product; phase 1 independent review and FeaturePresenceTest passed. |
| F1.05 | Stop queued social work, repurposing in `engine:tick`, scheduled listeners/planners/drafters, and social delivery selection. Handle retries and partially running jobs so stopping the product stops new social spend. | Implemented: pipeline starts/resumes, runners, listeners, engine ticks, retries and transports refuse retired social work; phase 1 full gates passed. |
| F1.06 | Preserve historical social data and shared assets/components. A historical read/export path is sufficient; do not build a new access tier or delete tables just to simplify the menu. Keep historical billing records intact. | Historical tables/content and shared assets retained; no historical billing deletion or new tier introduced. |
| F1.07 | Import selected existing pages with a bounded crawl and store editable snapshots. Preserve URL/canonical/locale identity and avoid duplicate tracking. | Implemented/tested: bounded same-origin discovery, immutable public snapshots, canonical deduplication and locale preservation. Exact editable CMS snapshots are implemented and verified separately in phase 4; public snapshots do not grant edit capability. |
| F1.08 | Show the editable fields and supported page types for the chosen CMS. Unsupported layouts receive an explicit assisted-handoff path. | Supported CMS fields/type and native feasibility are visible on page/proposal views; bounded WordPress core/custom service/article support and readable assisted fallback independently reviewed and locally exercised in Phase4. |
| F1.09 | Capture business facts with sources and owner confirmation. Ask for missing facts instead of generating replacements. | Implemented/tested: immutable sourced fact versions, owner confirmation, expiry/retraction and separate public snapshots. Visible draft/confirmation UI checked; no pilot fact confirmation fabricated. |
| F1.10 | Ingest settled Search Console page data and relevant query-to-page data for selected pages, with bounded pagination, data freshness, and incomplete-data states. | Implemented/tested: daily GSC page/query reads, settled windows, bounded pagination and partial/stale states. Real UI baseline read separately; production API connection remains unverified. |
| F1.11 | Retrieve an initial 28-day search baseline and a preceding comparable window where available. Purchase measurement begins prospectively after tracking is verified; missing historical tracking is unavailable, not zero. Record known campaigns, major site changes, and seasonality concerns. | A private manual baseline plus prior window was recorded. Historical purchases remain unavailable; campaign/site-change context awaits owner evidence. |
| F1.12 | Implement and validate actual purchase measurement from the source identified in F0. Use the standard GA4 `purchase` event where applicable, with a stable transaction ID, actual value/currency, and the service purchased. Deduplicate repeated confirmations and distinguish purchases from quote requests, unpaid bookings, and button clicks. | A durable signed outbox is implemented and locally tested, with a stable actual-sale identity and optional applicable GA4 events. Production migration/connection and real reconciliation remain unverified. |
| F1.13 | Reconcile test transactions with the booking/payment record; define how cancellations/refunds and first-time versus returning customers are recorded. Use an explicitly labelled manual reconciliation if the sale completes outside the measurable website journey. Do not fabricate landing-page attribution for unmatched sales. | Local tests and visible manual flow pass: deduplication, corrections, retained deposits, cancellation/refund balances, chronology and unmatched attribution. Production transaction reconciliation remains open. |
| F1.14 | Check GA4 dimension/metric compatibility and the landing-page attribution available from the real flow. Where source/session matching is unavailable, show purchase totals with unattributed sales separated. Document tracking gaps and the date reliable collection began. | GA4 compatibility handling tested; standard landing paths kept as property-path evidence with unknown origin. Never assigned to pages or added to paid-order totals. Reliable production start date unavailable. |
| F1.15 | Preserve a meaningful read-only experience when Google or the CMS is disconnected. Missing data must display as unavailable rather than zero. | Visible six-page disconnected Performance view and purchase empty state verified; missing figures show unavailable, not zero. |
| F1.16 | Do not infer “not indexed” from zero impressions. Treat crawlability, observed search impressions, and verified indexing as separate facts. | Implemented/tested: search appearance and unknown indexing separated; legacy nullable indexing labels fixed. |
| F2.01 | Start with three opportunity types: an existing page losing relevant search performance; an existing page with meaningful search exposure and an answer/intent gap; a service page missing a buyer-critical fact the owner can confirm. | Implemented and reviewed: fresh relevant search decline, query-supported answer gap, missing commercial fact; explicit quoted owner-review path supports sparse sites. PageOpportunitiesTest passes. |
| F2.02 | Rank with explainable factors: commercial relevance, observed search evidence, content gap, confidence, and implementation effort. Do not invent revenue forecasts or a precise expected uplift. | Implemented: visible commercial relevance, observed search, gap, confidence and effort factors. Low confidence and absent data stay explicit; no revenue forecast. |
| F2.03 | Show no more than five recommendations and allow “nothing worth changing” as a valid result. Low-volume sites receive an evidence-led review with lower confidence. | Implemented/tested: at most five open recommendations, stable dismissals and valid empty outcome; stale or unrelated data cannot force a rewrite. |
| F2.04 | Check whether a suitable page already exists before proposing a new one. Basic overlap detection is sufficient; automatic consolidation and redirects are deferred. | Implemented/tested: bounded same-locale overlap across up to 500 known pages, with title-match and inferred-locale limitations. Untracked candidates cannot silently become approved link targets. |
| F2.05 | Create a bounded proposal covering supported title, description, text-section, or internal-link changes. Preserve working content and provide an explicit reason for each change. | Implemented/tested: at most five exact title, description, text-block or internal-link edits. Immutable source/fact revisions and explicit reasons; unsupported or ambiguous changes rejected. |
| F2.06 | Display before/after text, sources, missing facts, and the measurement to follow. Pin the source revision and evidence used. | Visible local review shows before/after, pinned public source, confirmed fact versions, missing questions and measurement plan. No invented pilot facts. |
| F2.07 | Support accept, revise, and dismiss. Record review time and substantive corrections; a materially altered proposal must be reviewed again. | Implemented and visibly tested: acceptance, explicit revisions, reasoned dismissal, current-source reassessment and no-change outcome. Review time is consumed after successful actions; altered revisions clear approval. |
| F2.08 | During the pilot, provide a precise handoff for the owner/operator to apply the change. Record who applied it and when; export is not publication. | Implemented and visibly tested: separate publication authorization, exact handoff, named operator and actual-time field. Export is not publication. Live pilot application remains A17. |
| F2.09 | Fetch the live page after publication and verify the supported changes. Start follow-up measurement from the verified date. | Implemented/tested: actual public fetch checks identity, fields, blocks and captured forms/links/structure. Visible synthetic unchanged-page check failed correctly, with no follow-up created. Real pilot change remains A17. |
| F3.01 | Identify the exact CMS object and obtain its editable source and revision before proposing a write. | Implemented and independently reviewed: exact typed CMS identity, native source and revision pinned alongside public snapshot. Verified on real local WordPress and custom service/article fixtures. |
| F3.02 | Support a documented subset of pages/posts and metadata fields. Resolve ownership by SEO plugins and editors during the feasibility check. | Documented bounded WordPress core pages/posts, text/title/description/link fields with SEO ownership and unsupported-editor fallback; custom service/article codecs tested. Coupled title/teaser effects are reviewed explicitly. |
| F3.03 | Compare the current CMS revision with the reviewed snapshot immediately before publication. A conflict creates a new review requirement rather than overwriting an external edit. | Current source revision and approved dependency fingerprints checked before write; conflicts require fresh review. External-edit and changed-credential regressions pass. |
| F3.04 | Separate approval from publish permission. Apply only the approved fields; retain layouts, unrelated metadata, forms, URLs, and existing content outside those changes. | Separate acceptance and publication authorization; exact supported patch and structural public checks. Real service/article title and paragraph publication and recovery passed for both receivers. |
| F3.05 | Reuse delivery identity, retry, metering, and tenant controls. Ambiguous outcomes must be reconciled before retrying; repeated requests cannot duplicate content or changes. | Durable operation identity and immutable receiver receipts; duplicate delivery reconciles to the same outcome. Lost response and bounded committed-at receipt checks pass; original queued writes are not revived after credential rotation. |
| F3.06 | Save the original revision and provide recovery for a failed or unwanted change. Recovery must also detect subsequent external edits. | Guarded native recovery uses retained source and detects intervening external edits. Recovered timestamps preserve original publication history; real fixtures restored and publicly reverified. |
| F3.07 | Verify the public result and applicable metadata. A successful API response is insufficient to declare verification complete. | Public fetch checks exact approved effects plus retained source structure; a WordPress coupled title occurrence correctly failed verification before its compiler fix. Final four journeys per receiver all passed. |
| F3.08 | Offer a clear assisted fallback for unsupported content, expired credentials, and conflicts. | Visible readable assisted fallback for missing/unsafe connection, unsupported source or stale revision. A connection error no longer crashes the proposal review screen. |
| F4.01 | After the internal cycle works, recruit up to five external local businesses, initially favouring service businesses with comparable pages and booking/sales flows. Record alternatives, purchase-tracking readiness, and the reason each joins or declines. | Concrete external-evidence register saved in seo-geo-launch-evidence.md. Zero approached or recruited so far; contacts and explicit outreach authorization remain owner dependencies. No fabricated cohort. |
| F4.02 | Present a concrete page proposal and the provisional US$89 monthly offer, with explicit scope and actual checkout currency. Aim for at least three paid commitments before broadening beyond the initial supported workflow. Do not count an internal pilot as external demand or charge it to manufacture pilot revenue. | Version 2 concrete validation offer is US$89/four first-accepted page improvements, explicit USD currency and supported scope. No real external checkout/charge or paid commitment; demand and production price setup remain external. |
| F4.03 | Show each changed page's baseline, verified date, elapsed observation window, and follow-up readings. Begin reviews at roughly 14 and 28 days, extending when data is sparse. | Implemented/tested: immutable acceptance baseline, 14/28 full Pacific-day follow-ups after verification, three settlement days, event recovery and append-only later readings. Actual pilot elapsed windows remain external evidence. |
| F4.04 | Report actual purchase count and value/currency, with new-customer purchases separated where known. Show requests, bookings, and enquiries only as intermediate signals. Separate attributed, unattributed, and manually reconciled purchases; observed purchases do not establish incremental sales caused by Avyo. | Purchase ledger/report implemented and tested, preserving status, currencies, known/unknown customer type and attributed/manual/unattributed groups. New follow-up reports retain the same paid-order definition. |
| F4.05 | Use the same sale definition, transaction handling, and landing-page definition across the comparison. Missing consent, attribution loss, low volume, outages, refunds, and changed tracking remain visible limitations. | Implemented/tested: pinned source and tracking start, immutable baseline plus separate later refund restatement, changed-source/consent/sparse limitations; Pacific purchase boundaries covered in summer and winter. |
| F4.06 | Compare with suitable unchanged pages and wider site trends where possible. Report associations; do not attribute every before/after change to Avyo. | Implemented/tested: same-locale/kind/volume comparison candidates require later matching public evidence; fixed other-tracked-page cohort is explicitly not a whole-site total. No causal uplift claim. |
| F4.07 | Review AI visibility using stable prompts and recorded model/market/date context. Show mentions and citations separately. Sampled answers are not a census of real customers' AI activity. | Stable prompt sets and provider/model/market/date answer evidence implemented, with mentions and citations separated and full final text retained. Independent review approved after fixes; bounded live-provider check and final visible flow await explicit authorization. |
| F4.08 | Re-rank future work from measured evidence and explicit owner feedback. A dip must not automatically trigger another full rewrite. | Immutable owner outcome reviews and measured reassessment implemented; fresh evidence, retained decisions, valid no-change, recovery confounders and no automatic paid rewrite. Independent root review approved;125tests/869assertions scoped gate passed. |
| F4.09 | Test the provisional US$89 per-site package with a clear allowance for reviewed page improvements. Measure manual assistance separately from provider costs; revise scope or price if the cost of support makes a small discount unsustainable. | Owner-only delivery-economics records separate measured operator minutes/rates from provider receipts and owner review timers. Unknowns prevent complete cost scenarios. US$89/four improvements is a concrete initial package; actual support costs and demand remain unvalidated. |
| F4.10 | Version any new plan and quota semantics. Define what consumes an improvement allowance, what retries/revisions include, and how a new article is counted before selling it. Rejected proposals and technical retries must not create surprise charges. | Version 2 currency/quota contract and first-acceptance ledger implemented. Reject/no-change use zero; proposal revisions/retries/recovery never count again. Existing version 1 retained; new-article allowance is zero in v2. Billing regressions, including the no-subscription first-acceptance edge, passed in the final whole-product gate. |
| F4.11 | Publish the supported page types, integrations, trial terms, and scope clearly. Preserve existing purchased access and content. | Marketing, checkout price validation, billing and focused onboarding specify existing-page scope, representative WordPress/custom support, assisted limits and payment-method trial. Original subscriptions/content retained. Final visible release gate pending. |

## Additional acceptance requirements

| ID | Requirement | Evidence / status |
|---|---|---|
| A01 | Existing service page and existing article both complete the full journey without changing their public URLs | Real local WordPress and custom service pages and articles completed proposal → accept → publish → public verify → duplicate reconcile → recovery without changing canonical URLs. Production pilot remains A17. |
| A02 | Tenant isolation, canonical deduplication and locale preservation across reads, writes and reporting | Tenant/page/source constraints and canonical/locale checks are covered across pages, purchase intake, proposals, both receivers, AI accuracy and fact maintenance. Phase4 realjourneys and focused isolation regressions pass; final whole-product suite remainsPhase7. |
| A03 | Generated/article text cannot silently become confirmed business evidence | Verified in page/fact tests and source review: public/generated text never silently becomes confirmed evidence. |
| A04 | Changed proposal invalidates approval; approval alone never publishes | Verified in proposal regressions and visible browser: approval alone creates no publication; new revision clears acceptance; invalidated handoff needs explicit reauthorization. |
| A05 | Publication detects external edits, preserves unrelated content/forms/layout and reconciles ambiguous writes | Native/assisted tests and independent source reviews cover external edits, unrelated content/form/link preservation and ambiguous outcome reconciliation; actual form submission/visual layout remain outside structural verification. |
| A06 | Recovery detects subsequent edits and does not overwrite them | Native receiver and application regressions plus real guarded recovery passed; external edits prevent blind recovery. |
| A07 | Missing/partial analytics remain unknown; zero impressions do not imply not indexed | Verified in measurement tests and visible Performance view; GA4 origin ambiguity is explicit. |
| A08 | Purchases deduplicate, reconcile actual value/currency and refunds; no attribution or new-customer status is invented | Implemented and independently reviewed; local receiver/bridge tests and manual UI correction flow pass. Production proof remains A17. |
| A09 | Social direct actions, assistant tools, queued jobs, retries, listeners and transport do not run | Verified by phase 1 full tests and independent boundary review. |
| A10 | Historical content, assets, purchased access and billing history remain intact | Legacy subscription version, limits, renewal, currency and history preserved in billing tests; historical content/assets remain read-only where social is retired. Admin mixed-currency and incomplete-cost regressions passed in the final whole-product gate. |
| A11 | WordPress core pages/posts, metadata support, conflicts, retries, unsupported layouts and recovery exercised on a representative installation | Representative real WordPress fixture passed all21 receiver tests and application acceptance journeys; supported core service pages/posts, metadata ownership, conflict/retry/recovery and unsupported layouts covered. |
| A12 | Full AI answers retain model/prompt/market/date, distinguish mentions/citations, identify supported factual discrepancies and offer actionable own-page proposals/external handoffs with rechecks | Implemented and independently reviewed after stale-review, dispatch, unsupported-surface and paid-access fixes. Combined100tests/458assertions plus21tests/96assertions pointer/sampling regression pass. Final visible and live-provider validation pending. |
| A13 | Changed confirmed facts identify dependent tracked pages/proposals and produce reviewable updates | Fact maintenance implemented; current source/fact dependencies, changed/retracted facts, usage impacts, bounded proposals/assisted handoffs and explicit rechecks. Independent root review approved after constraints/dispatch fixes; 24 tests / 138 assertions passed, including the paused-project late-receipt regression. |
| A14 | Provider costs, operator time, allowances and retries/revisions accounted for in the price/package | Versioned package, owner-only immutable effort records and durable provider receipts implemented. Independent economics review approved after entitlement and duplicate legacy-pointer fixes. Actual production cost sample is unavailable; no sustainable-margin claim. |
| A15 | Tests, PHP/JS lint, formatting, static types, production build and visible Docker app checks pass | Final app/bin/test exited 0: 2,167 tests / 9,885 assertions, Pint 1,078 files, PHPStan, ESLint, Prettier, TypeScript and production build passed. Host full and final scoped static checks also passed. Earlier phase 1–4 visible checks stand; final populated phase 5/6 visual review awaits authorized sign-in. |
| A16 | Subagent critique after every completed phase, findings resolved or recorded with explicit external dependency | Independent critiques approved after resolved findings for phases 1–6; final bounded requirement and billing audits approved. Final combined automated gate passed. Permission-dependent visual/provider checks and actual pilot outcomes remain separate acceptance requirements. |
| A17 | Actual internal pilot publication, purchase reconciliation and baseline collection | A private owner-authorized baseline was recorded. Production purchase bridge installation, real-sale reconciliation, confirmed facts and first live page publication remain external dependencies; no production success is claimed. |
| A18 | External recruitment, paid commitments, four weekly cycles/renewals and 8–12-week outcome assessment | External evidence required; implementation cannot manufacture customers or elapsed observations |

## Current evidence and findings

- Initial worktree contained only the strategy documentation changes; no application implementation was present.
- Local app and Horizon are healthy at port 8091 after disposable Docker build-cache cleanup resolved Redis disk exhaustion. Data volumes were preserved.
- The private pilot's positive ledger receipts are the purchase source. Durable signed purchase bridge and correction/refund semantics are implemented and locally verified; production installation remains open.
- The private pilot's current article receiver is provider-specific; the existing custom webhook does not establish compatibility with that site.

## Review log

Phase 1 final verification on 15 September 2026: `app/bin/test` exited 0; Pint checked 873 files; PHPStan found no errors; all 1,899 tests passed (8,028 assertions); ESLint, Prettier, TypeScript and the production Vite build passed. Evidence log: `/tmp/avyo-phase1-final-gates.log`. The full run exposed two old calendar-sensitive tests; their fixture clocks are now explicit and their behaviour is unchanged. Visible checks also covered the new marketing/trial wording and website settings without automatic publishing controls. This final gate supersedes the interim run recorded in the source review below.

Phase 0 independent discovery critique by `focused_experience`: verify positive cleaning-revenue semantics when ledger entries are edited, not just `paid_at`; an article update must preserve effective content when its locale currently falls back; the new service-page receiver must own locking and audit because `ServiceOps::setContent()` does not. These findings are required inputs to the purchase and custom-publishing implementation. External access and live-pilot gates remain open.

### Phase 1 — independent engineering/product critique, 15 September 2026

Reviewer: `pilot_discovery` subagent, independent of all application implementation in this phase; used the engineering code-review skill. Source/diff review covered direct routes, assistant tools, pipeline starts/resumes and queued execution, publication/replay boundaries, onboarding claims, hidden data, channel quotas and empty states. No database tests were run by the reviewer while the root test suite was running.

| Finding | Resolution re-inspected in the current source |
| --- | --- |
| P1: approving an article or running the publication schedule could send it through a legacy automatic website channel without a separate Publish action. | `PublishToChannels::publishAutomatically()` now refuses the focused-product path. `FeaturePresenceTest::approval_waits_for_an_explicit_publish_even_on_an_automatic_channel` checks approval and scheduled publication create no delivery, while explicit publication creates one. Automatic controls were removed from website setup and its mutation endpoint refuses requests. |
| P2: hidden social channels could consume the website-connection allowance and leave an empty connection screen unable to add a website. | `ChannelController::store()` counts supported channels; regression test preserves the historical social row, allows the first website and refuses an extra website at the allowance. Social channel mutations/tests remain blocked. |
| P2: onboarding promised monthly planning and first drafts after launch although focused launch stops after research. | Wizard launch copy now describes website research and reviewing findings. Automatic social or monthly article drafting is not initiated by focused onboarding. |
| P2: Overview retained a monthly quota-planning action; its direct route/service could reactivate the retired planner. | Overview action removed; `ArticleController::plan()`, `MonthPlanner::start()` and `PipelineRunner` guard starts/resumes of `planning`. Regression coverage includes the month planner and cancelled retained planning jobs. |
| P2: retained social failures/in-flight jobs appeared as current website work and could hold onboarding in setup. | `PipelineRun::forActiveProduct()` filters retired pipelines and generic runs linked to social content. `WorkInFlight` and launch settling use it. `FocusedWebsiteLaunchTest::test_retired_runs_do_not_keep_the_overview_in_setup_or_show_a_social_failure` asserts retained history remains while a real website failure remains visible. |
| P2: the missing-metrics Analytics action opened publishing channels instead of Google setup. | Home now receives the project ID and gives separate website-connection and Google Analytics links; the latter opens the actual project's edit/setup page. |
| Follow-up from second review: `EngineTickCommand::isBusy()` still excluded pipeline names rather than generic runs linked to social content. | Fixed and re-inspected: the tick now uses `forActiveProduct()`. That scope and the runner's execution guard both resolve the effective content ID from the dedicated column or legacy input. The focused-tick test retains social `generation` runs using both identity forms and asserts website feedback can still start; the generic-pipeline test covers both forms at start and queued execution. |

Root's live local checks found four focused navigation items without social controls and a pilot website connection that was not yet a working existing-page receiver. No external test or publication action was invoked. Receiver setup remains an integration dependency. Root also verified healthy Horizon and Redis persistence after removing disposable Docker cache/old untagged images, without removing data volumes.

Review status: **phase-1 source review approved after fixes**. All identified material phase-1 issues were corrected and re-inspected; no remaining material issue was found in this review's scope. This is not a test-suite certification: root's final test/lint/build and live checks must be recorded separately before calling the whole phase complete. Root reported an interim full suite of 1,897 passes and two date-sensitive fixture failures, corrected by explicit test clocks, with rerun underway; no passing final gate is claimed here. Later planned tracked-page, fact, purchase, opportunity, CMS and measurement capabilities remain incomplete implementation work, not phase-1 regressions or a basis for claiming the goal complete.

F0 self-critique (not independent authorship): the discovered six-page inventory uses public snapshots with differing crawl dates; English is a provisional engineering choice without verified demand data; the first recommendation is a candidate rather than an owner-approved engine proposal; no representative WordPress site has been provisioned by discovery; production Google access and actual baselines/purchase tracking are unverified. Do not infer those acceptance items are complete from the discovery document.

### Phase 2 — independent purchase review, 15 September 2026

Reviewer: `focused_experience`, independent of the purchase intake/reporting implementation. Reviewed payload validation, signed intake, replay/revision handling, tenant references, attribution, reports, owner controls and regression tests. This entry records source review, not the phase's final verification gate.

| Finding | Resolution re-inspected in the current source |
| --- | --- |
| P1: partial payments and retained deposits after cancellation disappeared from monetary totals because only completed-sale cohorts were summed. | `PurchaseReport` now separately sums receipts, refunds and net balances across all recorded transactions. The purchase screen explicitly distinguishes these balances from the last-28-day completed-sale cohort. The retained-deposit regression covers partial and cancelled records. |
| P2: a legitimate refund could exceed receipts after a later ledger correction, causing the real refund to be rejected or lost. | The `reconciliation_required` state retains both actual amounts, permits a negative net balance and excludes the inconsistent record from completed-sale counts. UI copy flags the discrepancy. A correction-after-refund regression checks the preserved refund and negative balance. |
| P2: an observed landing alias was not attributed when the tracked page stored its canonical URL. | Public snapshots retain the normalized requested URL. `TrackedPageIdentity` resolves the canonical identity or a current public snapshot's attested requested/final alias within the tenant and current locale. Intake uses that resolver. A new end-to-end regression covers public capture, signed receipt, replay and page-attributed reporting. Old aliases unsupported by the latest public snapshot remain unmatched. |
| P2: service-level scoping did not prevent an accidental direct database write mixing a purchase's tenant, source, page or event record. | The composite-reference migration binds source/project, page/project and event/source/record identities. Direct database regression cases exercise cross-project and same-project cross-source rejection. |

The root reported 16 purchase tests and 128 assertions passing before the additional alias regression. The reviewer did not run database tests while the Google-measurement agent held the shared test lease. The new alias test and complete phase gate remain pending; no production collection or live purchase reconciliation is claimed by this review.

### Phase 2 — independent private-pilot bridge review, 15 September 2026

Reviewer: `focused_experience`, independent of the bridge implementation. Reviewed the applied sibling checkout; did not run its real application, scheduler, messaging or outbound delivery. Private patch evidence is retained locally.

| Finding | Resolution re-inspected in the applied source |
| --- | --- |
| P2: booking-ID order could classify a customer as returning even when the supposed earlier sale was paid later. | `PurchaseBridge::snapshot()` now requires an earlier evidenced purchase date for another paid/refunded sale on the same verified account. Uncertain history remains unknown. The regression reverses booking and payment order and re-captures the earlier sale. |
| P1: a GA4 refund accepted before a lost response could be sent twice by automatic retry, including after a later sale revision. | Google refund attempts are committed before transport. `sending` and `ambiguous` attempts block later refund sends. Explicit reconciliation records a verified received/not-received outcome and evidence, shares the delivery lock, and refuses attempts that may still be in flight. Tests cover response loss, later revisions, interrupted attempts and both resolution paths. The signed Avyo transport retains its separate replay-safe event identity. |
| P2: only the refund command validated dates; shared service writes and ledger edits could count future refunds immediately. | Shared create/edit operations validate the actual refund date, and reconciliation excludes future-dated refunds as it does receipts. The follow-up restoration fix subtracts an old refund only when it contributed to the current total. Its regression refuses restoration beyond available receipts and permits the exact remaining amount after another actual refund. |
| Attribution definition needed clarification. | The bridge documentation now defines the stored URL as the first observed consented public page. Consent after navigation does not establish the original session entry page or GA4 landing page. Avyo displays a matched-page relationship and does not invent channel attribution. |

The GA4 retry review follows Google's documented [purchase transaction-ID deduplication](https://support.google.com/analytics/answer/12313109?hl=en); the implementation does not extend that guarantee to [refund events](https://developers.google.com/analytics/devguides/collection/protocol/ga4/reference/events#refund).

The bridge author reported 26 targeted tests and 159 assertions passing after the final refund-capacity correction; the reviewer re-inspected the applied fix and regressions rather than running another database suite. **Independent purchase/bridge source review approved after fixes**: all material findings in this review were resolved, including the follow-up correction. The full isolated sibling test suite and the root phase gate remain separate verification work. No live installation, Google processing, source verification or production purchase is implied.

### Phase 2 — independent pages, facts and measurements review, 15 September 2026

Reviewer: `pilot_discovery`, independent of the Avyo page/fact and Google-measurement implementation. Used the engineering code-review skill to inspect tenant and owner boundaries, URL/network validation, source snapshots and fact confirmation, measurement completeness, attribution, historical rows and the visible report. The private-pilot bridge was authored by this reviewer and is covered by the separate independent bridge review above.

| Finding | Resolution re-inspected in the current source |
| --- | --- |
| P2: capturing a newer public snapshot left the detail view displaying the previously selected snapshot. | The capture form in `site-pages/show.tsx` selects the returned newest snapshot only after a successful capture. A chosen historical snapshot stays selected until then; the retained history remains available. |
| P2: combining GA4's session landing path with `hostName` could invent page attribution when a visitor landed on one website and purchased on another website in the same property. API compatibility alone did not prove the landing origin. | `ReadsLandingPurchases` now requests only `date`, `landingPagePlusQueryString` and `sessionDefaultChannelGroup`. It records selected property paths with `landing_origin: null`, never reconstructs a website URL and never claims page ownership. `PropertyAnalyticsMetric` has no page reference. `SynchronizePageMeasurements` writes the new `ga4_property_landing_paths` source separately, and `PagePerformance` reads that source into `report.analytics`; each page's GA4 purchases remain explicitly unavailable. The UI shows a separate property-path section with unknown-origin, selected-path and supplementary-evidence wording. |
| Preserve the unsupported historical observations without allowing them to certify the corrected report. | Migration `2026_09_15_130000` retains old page-analytics rows and marks their reads `ga4_legacy_unverified_origin`. The current reporting/synchronization code no longer consumes `PageAnalyticsMetric` or the old source. A fresh complete read is needed for the new source, and incomplete reads preserve earlier new-source observations with a stale warning. |

The attribution finding follows the distinct dimension definitions in Google's [GA4 Data API schema](https://developers.google.com/analytics/devguides/reporting/data/v1/api-schema) and its [cross-domain measurement documentation](https://support.google.com/analytics/answer/10071811?hl=en). The unsafe reconstruction was an inference from those documented scopes, not evidence that a specific pilot purchase was misattributed.

Source regressions re-inspected include the exact three-dimension request, unknown landing origin, attested alias-path selection, two different websites sharing one path without either receiving a page purchase, and preservation of earlier observations after a partial read. Missing observations stay unknown; property events are not added to the signed paid-order ledger or counted as new customers. No remaining material issue was found in this review's Phase 2 scope. **Independent pages/facts/measurements source review approved after fixes.**

This reviewer did not run database tests while the measurement author held the shared test lease. The author's targeted checks and the root's final test/lint/build gate must be recorded separately; this source approval does not certify those pending runs. Real Google-property access and processing, installation of the disabled-by-default private-pilot bridge, owner verification of its purchase source, the actual pilot baseline, elapsed measurement windows and any SEO/GEO or customer-growth outcome remain unverified. Later opportunity, publication/rollback, CMS and AI-answer capabilities remain planned work, not completed Phase 2 features.

## Open decisions and external dependencies

Collect decisions here while continuing independent implementation. Do not convert absent credentials, real customer commitments or elapsed outcome windows into fake data or a claim of completion.

- Production Google property access, permission to apply the first selected live page change, and purchase tracking start date must be verified.
- Version 2 validation scope is implemented at US$89 with four first-accepted improvements; existing subscriptions retain version 1. Actual provider-price setup, support cost and paid external rollout still require the evidence and owner decisions in seo-geo-launch-evidence.md.
- No external cohort exists yet; no customer messages or purchase commitments may be fabricated.
- Actual dates and weekly human support capacity were not specified.

### Phase 2 — final combined verification, 15 September 2026

`app/bin/test` exited 0: 1,960 tests and 8,423 assertions; Pint, PHPStan, ESLint, Prettier, TypeScript and production build passed. Log: `/tmp/avyo-phase2-final-gates.log`. Independent reviewers approved after attribution, deposit/refund, tenant-reference and snapshot-selection fixes. Visible local checks covered source-unavailable purchase reporting, isolated manual €125 partial payment corrected to one paid sale, draft facts, selected canonical page capture, and the six-page disconnected Performance view. The isolated synthetic project and records were removed. A second public capture selected the newest snapshot while retaining the previous one. The real Search Console baseline was read with explicit owner permission and recorded separately; it does not imply OAuth integration or verified production purchase collection.

### Phase 3 — independent opportunity and proposal review, 15 September 2026

Reviewer: `pilot_discovery`, independent of these implementations, using the engineering code-review skill. Reviewed opportunity diagnosis/reconsideration, immutable proposal and fact evidence, tenant/owner boundaries, generation and stale actions, the review UI, assisted publication and public verification. No application source was edited by this reviewer.

| Finding | Resolution re-inspected |
| --- | --- |
| P2: owner-entered questions could not be reconsidered after dismissal; diagnosis/reconsideration could compute from source outside the serialization boundary. | Explicit reconsideration validates current quoted evidence and preserves the earlier dismissal reason. Root moved source/protected-page computation inside the project transaction and reloads/locks page identities. Existing-page overlap includes known untracked candidates without presenting them as verified link targets. |
| P1/P2: matching extracted text could certify a page after a booking form was removed or an existing link changed. | `PublicPageStructure` checks captured element/attribute/form/link preservation, permitting only the approved text/insertion/link change and known rotating hidden anti-CSRF values. Regressions cover a removed form, changed existing destination and changed markup. The result explicitly excludes visual layout, script behavior and actual form submission from its claim. |
| P2: stale accept/revise requests and fact invalidation could revive a dismissed proposal. | Dismissed/generating proposals reject stale review actions; fact invalidation retains dismissal. Explicit reconsideration and fresh generation remain separate actions. |
| P2: an internal-link destination could be paused or change identity after the revision was prepared. | Acceptance and publication revalidate the saved target ID, canonical URL, tracking and locale. Historical approved evidence is retained. |
| P2: the review UI submitted cumulative seconds repeatedly. | Successful review actions consume only the submitted interval; validation errors and time accumulated during a request are retained. A zero-edit correction now has an explicit no-change explanation field. |
| P2: a publication read could reactivate a page paused while its HTTP request was in flight. | `TrackedPages::capture()` requires the same currently tracked identity at commit, using project/page locks shared by pause. Both authorization and verification use this capture-only path. The regression pauses during each read and checks no new snapshot, reactivation or verification. |
| P2: accepting an invalidated revision again restored its handoff without another publication decision. | A `review_required` publication rejects handoff/application. Explicit authorization fresh-checks the source before reactivating only an unapplied publication and adds an immutable authorization review record. The UI exposes that separate action. |

**Independent Phase 3 opportunity/proposal source review approved after fixes.** No material finding remains in this review's scope. Reinspection also confirmed bounded/no-change outcomes, current confirmed fact requirements, exact revision approval, preserved historical evidence, and the separation of accept, export, application report and public verification. Explicit changed-source regeneration pins a new snapshot/current measurements without rewriting the original diagnosis.

The proposal author reported the final focused gate passing: 41 pages/proposals tests / 374 assertions, including 20 proposal lifecycle cases and the final capture-race/reauthorization regressions; scoped PHPStan, Pint, TypeScript and ESLint also passed. This reviewer re-inspected those regression sources, did not run shared database tests, and did not independently certify the frontend timer in a browser. The combined root gate remains separate. Source approval does not certify native CMS delivery/recovery, production publication, actual form behavior, elapsed follow-up observations or customer-growth outcomes. The separately authored WordPress receiver package has its own independent review and fixture evidence; the main native adapter and private custom integration remain Phase 4 work.

### Phase 3 — root measurement review and final verification, 15 September 2026

Root independently reviewed the follow-up implementation by `social_shutdown`. Pinned approval baselines, fixed observation windows, Search Console property continuity, purchase-source continuity, later refund restatement, unchanged-page evidence, wider tracked-page scope and recovery of an interrupted verification notification were inspected. The review identified timezone-sensitive timestamp bindings: PurchaseReport now preserves ISO offsets, and follow-up snapshot/publication queries use UTC instants at exclusive next-Pacific-midnight boundaries. Summer and winter boundary regressions pass. The author reported 49 measurement/integration tests with 273 assertions; root's 30 opportunity/purchase tests passed with 235 assertions. No material issue remains in this bounded measurement source review.

The combined `app/bin/test` run passed **2,006 tests / 8,888 assertions**, PHPStan, ESLint, Prettier, TypeScript and production build. Its initial exit was 1 solely because four WordPress receiver files needed canonical formatting. Those files were formatted; the fixture seeder invocation was corrected to load its strict-types file normally; all 21 real receiver tests passed again. The final full Pint rerun passed all 972 files with exit 0. Logs: `/tmp/avyo-phase3-final-gates.log`, `/tmp/avyo-phase3-pint-final.log`, `/private/tmp/avyo-wordpress-canonical-integration.log`. No unrelated application code changed during the receiver correction, so the already-passing full application suite was not needlessly repeated.

Visible local verification used an isolated, explicitly synthetic project and an unchanged public example page: exact before/after and evidence display; acceptance without publication; separate handoff authorization; operator report marked applied/unverified; public fetch correctly failed the unmade title change while preserving other checks; edited revision 2 cleared approval and retained revision 1; Performance contained no fabricated follow-up. The synthetic project and all its rows were removed. The application-time control now accepts seconds, matching the strict authorization boundary. The private pilot was restored as the selected project. Its unresolved service ambiguity remains a question, not an owner-confirmed rule or a published change.

Phase 3 software/local verification is complete. Native publication, actual production pilot evidence, external commitments and elapsed outcome windows remain separate requirements; their absence is not represented as completed evidence.

### Phase 4 — independent native publication review and real acceptance, 15 September 2026

Root reviewed the adapter, compiler, authorization, reconciliation, public verification and recovery implementation independently from `focused_experience`; custom receiver implementation by `pilot_discovery` was separately inspected by root and the native adapter author. Material findings were corrected before approval: native/public source identity, same-page recovery references, original fact pinning during reassessment, missing facts on manually edited patches, coupled WordPress title occurrences, Gutenberg comment handling, immutable bounded receipt timestamps, credential-rotation recovery, and readable unsafe-connection fallback.

Final focused gate: **54 tests / 405 assertions** (17 native, 21 proposal, 16 custom codec), with scoped Pint, PHPStan, TypeScript, ESLint and production build passing. The real WordPress receiver suite passed 21 tests; the custom application's full gate passed 1,353 tests / 14,121 assertions, followed by 15 focused receipt regressions / 81 assertions after timestamp checks.

Root then ran the actual application against representative local HTTP receivers. Service and article paragraph and title changes each completed approval, separate publication authorization, public verification, duplicate reconciliation, guarded recovery and public recovery verification. The earlier WordPress title attempt correctly failed when related title appearances were omitted; it remains in history and its guarded recovery passed. The compiler now presents those coupled effects in the next reviewed revision. Root also inspected the visible proposal with retained before/after, publication and recovery timestamps and a readable disconnected-state warning.

**Independent Phase 4 review approved after fixes; software/local gate complete.** Isolated synthetic projects retain review evidence and both fixture sources were restored. No production page was edited. Zero-second fixture reviews are not operator-effort evidence. Supported structural verification does not assert live booking-form success or visual layout equivalence. The whole-application final gate remains Phase 7.

### Phase 5/6 — interim review progress, 15 September 2026

Root independently reviewed F4.08 outcome cycles, pinned observation windows, explicit owner feedback, recovery confounders and version 2 audit-only onboarding. No material source finding remains in that scope. Author gate: 125 tests / 869 assertions, scoped PHPStan and frontend formatting/lint passed. Root's focused-offer and delivery-economics gate passed 136 billing tests / 466 assertions, including version 1 renewal, provider price lookup, version 2 USD checkout, allowance idempotency and rollback, tracked-page limits, unknown operator rates, append-only corrections and tenant isolation. Logs: `/tmp/avyo-focused-billing-final.log`, `/tmp/avyo-economics-static.log`, `/tmp/avyo-economics-types-final.log`.

Independent billing critique by `pilot_discovery` found that the new paid workflow entry points and queued workers did not consistently recheck subscription access. Each affected proposal, sampling, accuracy and maintenance workflow is being corrected at kickoff and before paid requests. Provider receipts are being reconciled with step totals so an interrupted rollup cannot hide known expenditure; unknown charges remain explicitly incomplete. Those changes need their own final regression and source gate before Phase 5/6 is complete.

Root's independent fact-maintenance review identified dispatch serialization and missing composite source/tenant constraints. The author fixed both, added migration 191000, and passed 24 tests / 136 assertions plus scoped static/style checks. Reinspection confirms exact page/snapshot/check identities, current confirmed facts, quote offsets, retained usage impacts, explicit replacement facts, assisted unsupported-surface correction and non-causal recheck wording. The author is adding one final paused-project late-receipt regression. Full-answer accuracy critique separately identified stale review decisions, concurrent dispatch and unsupported owned-page surfaces; fixes and final reinspection are in progress.

Visible checks requiring a new local seeded-account sign-in and the bounded real-provider validation remain pending explicit user authorization. Automatic approval review rejected those attempts; no rejected sign-in or paid request ran. No production edit, customer outreach, purchase commitment or fabricated outcome is included in these test claims.

### Claim correction dependencies and admin currency review closure — 15 September 2026

`focused_experience` independently inspected root’s `RecordClaimOpportunity` and the AI-answer/fact-maintenance callers. The helper locks current project/page/fact identity, checks the exact saved public quote and fresh canonical/locale snapshot, and retains idempotent origin evidence without approving or publishing a change. No material issue remains in that helper’s reviewed source scope.

The integration review found a dependency gap in the proposal implementation: a generated patch could cite a different confirmed fact while the original reviewed discrepancy fact remained only in the diagnosis JSON. That original fact changing could then escape revision invalidation. The proposal author corrected this by retaining the original reviewed fact IDs alongside the edit’s cited IDs in immutable revision references, including owner-requested reassessments. All those dependencies must remain current before the worker’s paid request and before creating any generated or manual revision. Regressions confirm that an original fact change invalidates acceptance and publication, and that a manual revision cannot evade the originating workflow’s recheck. Historical revisions and references are retained.

Root’s admin review also identified two reporting errors: mixed EUR/USD plan fees were summed, and USD contributions could remain numeric despite unknown provider charges. Admin reports now group monthly list fees by each stored plan’s currency, identify recorded usage as USD, and show contribution as unavailable for cross-currency or incomplete provider evidence. These are monthly list fees and known usage subtotals, not received revenue or a full margin including support/overhead. The shared spending summaries preserve completeness while performing each provider reconciliation once per project. Version 1 prices remain unchanged.

Executed author gate: 58 tests / 631 assertions across AdminPanelTest, PageProposalsTest and FocusedOfferTest, including the final no-subscription first-acceptance guard, mixed-currency rows/detail, unknown provider cost and reviewed-fact dependency cases. Scoped PHPStan, TypeScript and ESLint passed. The preceding F4.08/onboarding/opportunity/native/proposal gate passed 125 tests / 869 assertions. Root’s combined whole-application gate and final independent reinspection remain separate; this note does not claim new live UI validation, a paid provider run, production publication or measured customer growth.

### Phase 5/6 — source review closure, 15 September 2026

The interim paid-access, durable-receipt, full-answer accuracy and fact-maintenance findings above are resolved in source and their focused regressions. Root independently re-inspected the original reviewed-fact dependency guard and admin currency/completeness changes. `pilot_discovery` approved the economics review after unknown-charge and duplicate legacy-pointer fixes. Full-answer accuracy now preserves explicit dismissal/reopening history, current fact references, exact quotes, bounded owned-page opportunities or precise assisted handoffs, separate rechecks and dispatch recovery. Its combined checker/sampling/cost gate passed 100 tests / 458 assertions, followed by 21 tests / 96 assertions for final receipt-pointer and sampling cases. Fact maintenance passed 24 tests / 138 assertions, including late receipts on paused projects. These focused suites overlap and must not be added together as a distinct total.

Clearly labelled synthetic AI accuracy and fact-maintenance projects are prepared and paused for the remaining visual gate; their guides are `ai-accuracy-local-fixture.md` and `fact-maintenance-local-fixture.md`. They contain fabricated examples only, with zero real provider requests. Preparing them did not sign in or change the selected project. Authenticated visual review and bounded paid-provider validation remain pending the two explicit authorization questions; no alternate authentication or paid execution was used.

### Phase 7 — requirement audit and initial whole-product gate, 15 September 2026

`pilot_discovery` independently audited A01–A18 and F0–F5 and found no additional missing or unwired included software capability within that bounded review. The demand scorecard retains the original denominator: at least three paid commitments from five businesses approached. Private-pilot patch evidence and instructions are retained locally; historical incremental patches must not be reapplied.

The first final run (`/tmp/avyo-phase7-full-gates.log`) exposed three failing tests among 2,163, three PHP formatting issues, one frontend formatting issue and one PHPStan model-generic error. The stale public-price tests now assert the current version 2 USD offer. The new effort model follows the same non-final Eloquent pattern as the shared tenant trait. Formatting was corrected. Host full PHPStan then passed (`/tmp/avyo-phase7-host-static-final.log`), and seven marketing/effort tests passed with 78 assertions.

The sampling timeout failure exposed a real consistency issue. Sampling now derives a shared deadline from the actual inventory and answer transport bounds, with a margin; stale detection waits for the deadline plus grace. Worker, reconciler, report and explicit-recheck guard use that same boundary at database timestamp precision. A conditional stale update preserves a receipt arriving between read and update. Root reviewed the change independently and all 20 timeout/sampling tests passed with 205 assertions (`/tmp/avyo-sampling-deadline-tests-final.log`). The default is a 360-second step and 420-second stale boundary, below the 2,100-second expensive worker timeout. Config changes are evaluated using the current configuration.

The combined final rerun passed. Passing source and automated gates alone does not close the remaining visual, provider or actual pilot/outcome requirements.

Final bounded independent billing review by `focused_experience` approved the versioned catalog/Stripe price resolution, atomic first-acceptance quota, immutable effort corrections, incomplete-cost economics, current public offer and launch register. Its only remaining copy observation was corrected: past-due messaging no longer implies automatic publication. Scoped Pint passed after that text-only correction. The reviewer performed no new account access, external action or database test. Root also passed host static analysis of the final sampling changes (`/tmp/avyo-phase7-host-sampling-static.log`).

### Phase 7 — final automated result and remaining acceptance, 15 September 2026

`app/bin/test` exited **0**. All **2,167 tests / 9,885 assertions** passed. Pint checked **1,078 files**; PHPStan found no errors; ESLint, Prettier, TypeScript and the production Vite build passed. Final log: `/tmp/avyo-phase7-final-gates.log`. The final payment-message correction was in place before its billing tests ran and independently passed Pint. `git diff --check` passed. No further application source changed after this successful gate.

All included software is implemented and independently reviewed within the documented scope. Final sign-off remains open: populated phase 5/6 visual checks need the pending local seeded-account authorization, and real provider checks need the pending bounded-spend authorization. Automatic approval review rejected those specific actions for missing explicit account/spend approval; neither ran, and no alternative route was used. The prepared synthetic evidence does not substitute for either check.

The implementation goal also retains real-world dependencies: confirm the Deep Clean inclusion rule, configure/install the production integrations, reconcile a real paid sale, review and publish the first actual page improvement, then collect elapsed observations and authorized external-cohort evidence. These are listed concretely in `seo-geo-launch-evidence.md`. There are zero recorded external approaches, paid commitments or renewals, and no production purchase or growth result is claimed. The goal is not marked complete.

### Later content correction and authorized acceptance closure — 15 September 2026

The owner subsequently rejected the existing-page-first experience and explicitly chose automatic scheduled articles with an option to review first. [Content autopilot correction](content-autopilot-correction.md) records the implemented manager workflow, article engine, current version 3 offer, scheduling, WordPress/custom article receipts, business grounding, independent critiques and current gates. This supersedes the earlier product-direction entries, while preserving their technical history.

The owner also explicitly approved both previously blocked validation actions. The existing authenticated local session was used to inspect Dashboard, Calendar, Content, Results and the populated AI-accuracy/fact-maintenance fixtures. Schedule pause, review-mode change, time change and approval persisted without browser publication. The paid experiment completed four short provider answers and one synthetic metered fact check for **US$0.110590**, with no retries or unknown charges. The synthetic project is paused, and local workers remain stopped. See [provider validation](ai-sampling-validation.md).

The earlier references to pending account/provider permission are historical and now closed. Production connections, approved real content, actual purchases and elapsed outcomes remain separate external pilot work. The software correction and its local acceptance are complete; no production activation or market validation is claimed.
