# Receiver fixture validation — 15 September 2026

## Article publishing correction (0.2.0)

The historical existing-page results below remain evidence for that API. Plugin 0.2.0 adds `articles.php`; the old ZIP and two-file source hashes below are historical and no longer describe the current package.

- Final formatted receiver source: **29 real HTTP/database tests passed**, 38.441 seconds, exit 0 (`/private/tmp/avyo-wordpress-0.2.0-tests.log`). This combined run includes all eight article cases and all 21 existing-page cases.
- A real Avyo calendar journey also passed (`tests/avyo-journey.php`, `/private/tmp/avyo-wordpress-article-journey.log`): verified connection, no early dispatch, due-time canonical approval, actual HTTP publication and public page, completed calendar status, one delivery identity, one article allowance unit, no duplicate after retry, and automatic measurement enrolment without fabricated observations. The synthetic fixture project was paused on completion; paid model calls: zero.
- Installable current ZIP: `/private/tmp/avyo-receiver-0.2.0.zip`, SHA-256 `ebea7f7912271a7ae0a201dabe55d64400fc21286bfdff0f310cca740841c98c`. It contains only `avyo-receiver.php`, `receiver.php` and `articles.php` under `avyo-receiver/`; no fixture credentials or tests.
- Current production source SHA-256: `avyo-receiver.php` — `a9418f0ee11498fcfd81d3f1e13e84f38763bffc682e69963ee37bfa2325fea6`; `receiver.php` — `ed518db3479c7dc55d4c6e9adfdff748ca5bf8811423ab21aa60db43549d1d46`; `articles.php` — `6daa96fa575f4be60df84431b1fb01e4586e1bbfddabc76a6f3926bfe7c8b325`.

- Eight new real HTTP/database article tests passed, 10.282 seconds, exit 0, in `/private/tmp/avyo-wordpress-article-tests-rerun.log`. They exercise connection capability, permission, create and public HTML/description, repeat delivery, unchanged-content delivery, update with stable URL/author, external-edit conflict, reused identity, invalid HTML/language, concurrent duplicates, filter rollback, failed transactions and failed post locks.
- The 21 existing-object tests also passed in the combined run recorded in `/private/tmp/avyo-wordpress-article-tests.log`; that run had one new test assertion failure because WordPress inserts harmless whitespace between public paragraphs. The article assertion was corrected to verify each exact public heading/paragraph separately; stored source bytes remain checked exactly. All eight article tests passed on the rerun.
- Current application transport/enrolment validation and independent review are recorded in the content correction ledger. No production site, paid AI provider or production publishing schedule was used in these receiver tests.

## Historical existing-page validation


Author validation, separate from independent source review and the main Avyo adapter/workflow gate.

- **21 real HTTP/database tests passed** after canonical project formatting, 27.656 seconds, exit 0. Log: `/private/tmp/avyo-wordpress-canonical-integration.log`. This includes actual core writes/revisions, concurrent revision conflicts, deliberately discarded HTTP response reconciliation, recovery and external-edit protection, external edit/revert detection, unchanged forms/attributes/excerpts/metadata, bounded links/insertion, malformed requests, WordPress filter rollback, duplicate permalink refusal, real Yoast ownership and actual SQL failures for transaction start, every lock and required source/operation reads. Those failures returned 503 and preserved both the source and the absence of a committed operation.
- Runtime: official WordPress **7.1**, PHP **8.3**, MariaDB **11.4**, **Twenty Twenty-Five 1.5**, **Yoast SEO 28.5**. Yoast was deactivated after its ownership test. The fixture remains available at `http://localhost:8093`, with its separate named volumes retained. No Avyo database tests or CleaningPoint changes were made.
- Browser inspection showed the public **Reviewed cleaning service** heading, the approved “cover the kitchen and bathroom” wording, and the original Name input / Request a visit button. The document title included the site suffix, confirming why it must not be equated directly with raw `post_title`. Public HTML assertions also checked the single approved description and retained form markup.
- PHP syntax, canonical app-configured Pint on all four package PHP files, README/Compose Prettier checks, shell syntax and `git diff --check` passed. Canonical Pint adds strict types and orders methods; the real suite passed with those changes. WP-CLI `eval-file` cannot load the strict seeder, so `fixture/start.sh` uses `eval` to require it as a real PHP file; that exact invocation was tested successfully. The successful-path WordPress debug-log check returned no entries. The final failure-injection tests intentionally produce SQL syntax errors in the fixture log; these are expected evidence of wpdb failure handling. Initial setup CLI warnings were corrected before these checks.
- Installable ZIP: `/private/tmp/avyo-receiver.zip`, containing only the two production plugin files under `avyo-receiver/`. Fixture credentials, MU guards, tests and seeder are excluded. ZIP SHA-256: `62f666f2a79a26d34e87c5d78cad4bbf07066056d25a5e820c2b027b36e3100c`.
- Tested source hashes after canonical formatting: `plugin/avyo-receiver.php` — `b3bfd837c8770a65f5c6fc22f338c7af15ad47697cb0a42f4213dea0bc28667c`; `plugin/receiver.php` — `a9fa7235dbea97553e0b405db7b52e4e318ba4ddc96a6afb4362ccd37261b2f1`.

Independent root review found that unchecked wpdb transaction/lock results could continue after a failure. The author fixed this by checking required statements and reads, returning unavailable/unknown outcomes rather than plausible snapshots or missing operations, and adding real fault-injection regressions. **Independent source reinspection approved after the fix** by the root reviewer: required transaction/lock/source/operation reads were re-inspected along with the 21 real regression tests and their no-write/no-journal invariant. No further blocking finding remains within the documented receiver boundary. This approval does not certify the separate main adapter or complete the CMS phase.

These checks validate the documented local receiver boundary, with independent source review recorded above. They do not complete F3/A11: the main Avyo source/approval/publication adapter, its tenant and quota handling, its real public-result verifier, and the combined user journey remain separate requirements. Production installation, credentials, caches, custom hooks, unsupported themes/builders and SEO-plugin metadata editing are not certified by this fixture.
